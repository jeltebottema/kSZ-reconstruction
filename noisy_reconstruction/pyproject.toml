[project]
name = "noisy-reconstruction"
version = "0.1.0"
description = "Dual QE + ML reconstruction pipeline under realistic noise."
requires-python = ">=3.11"
dependencies = [
    "ksz-core",
    "noise-analysis",
    "21cmsense",
    "tuesday-eor",
]
